<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="BasicTextView">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:fontFamily">@font/roboto</item>
    </style>

    <style name="ListIemTextViewTheme" parent="BasicTextView">
        <item name="android:layout_marginLeft">@dimen/card_textView_marginLeft</item>
        <item name="android:layout_marginTop">@dimen/card_textView_marginTop</item>
    </style>

    <style name="DetailActivityTextViewTheme" parent="BasicTextView">
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:drawableTint">@color/iconColor</item>
        <item name="android:drawablePadding">@dimen/TextViewDrawablePadding</item>
        <item name="android:textColor">@color/iconColor</item>

    </style>

    <style name="ActionIconViewTheme" parent="DetailActivityTextViewTheme">
        <item name="android:gravity">center</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:drawablePadding">@dimen/dp_6</item>
        <item name="android:textSize">@dimen/description_textSize</item>
    </style>

</resources>
